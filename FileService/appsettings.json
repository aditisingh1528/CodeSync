{
  "ConnectionStrings": {
    // FileDB is SEPARATE from AuthDB and ProjectDB — each microservice owns its data
    "FileDB": "Server=LAPTOP-64SKNE7A\\SQLEXPRESS01;Database=FileDB;Trusted_Connection=True;TrustServerCertificate=True;",
    // Same Redis instance as ProjectService — keys are namespaced so no collision
    "Redis": "localhost:6379,abortConnect=false"
  },

  // Must match AuthService appsettings.json exactly

  "RabbitMQ": {
    "HostName": "localhost",
    "Port": 5672,
    "UserName": "guest",
    "Password": "guest",
    "ProjectDeletedQueue": "project.deleted",
    "RetryDelayMilliseconds": 5000
  },
  "Jwt": {
    "Key":      "ThisIsASecretKeyForJwtTokenGeneration@AuthService2024!",
    "Issuer":   "AuthService",
    "Audience": "AuthServiceClients"
  },

  // Cache expiry — same strategy as ProjectService
  "CacheSettings": {
    "SlidingExpirationSeconds":  120,
    "AbsoluteExpirationSeconds": 600
  },

  "Logging": {
    "LogLevel": {
      "Default":              "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
